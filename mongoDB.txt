DIFFERENCES & SIMILARITIES BETWEEN MONGODB & SQL:
	-Differences:			* MongoDB is a noSQL language, therefore its philosophy lies in Schemaless formats,
					  Therefore,
						** No JOINS, we can stack data upon data
						** SCHEMALESS, we don't need to adhere to structured schemas, we can add dynamic properties

	-Similarities:			* MongoDB is installed as a server that listens to connections made by a language
					* MongoDB's installation has a shell to make querys outside of a language
					* MongoDB = MySQL Structure:
						** Database = Database
						** Collections = Tables
						** Documents = Entries  (in MongoDB Documents are in JSON format)

LANGUAGE INFO:
	-Drivers:			* (MongoDB.com -> Docs -> Guides -> MongoDB Drivers) -
					  Has information on how to use MongoDB with the language of your choice.

BASIC QUERIES:
	-SHOW DBS			* Shows the Databases

	-USE someDB			* Uses the named Database (also creates it if it doesn't exist)

	-db.someTable.			* db - refers to the current Database beign used.
	 insertOne({}):			* someTable - is a collection (table) we'll base our query on
					* insertOne() - is a query function

	-db.someTable.			* find() (w/ no arguments) - shows us the entire contents of the collection
	 find():			* Append .pretty() - to make the JSON "prettier"
